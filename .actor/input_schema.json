{
  "title": "App Scraper Actor",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "action": {
      "title": "Action to perform",
      "type": "string",
      "description": "Specify the action to perform, How do you want to Scrap the Data?",
      "editor": "select",
      "enum": ["LIST_APPS", "LIST_DEVELOPER_APPS", "GET_DETAILS"],
      "default": "LIST_APPS"
    },
    "platform": {
      "title": "Choose the Platform",
      "type": "select",
      "description": "Choose the Platform needed to be scrapped",
      "editor": "select",
      "enum": ["GOOGLE_PLAY", "APP_STORE"],
      "default": "GOOGLE_PLAY"
    },
    "appId": {
      "title": "ID of the app needed to be scrapped (Works only if action choosen is GET_APP_STORE_APP_DETAILS or GET_GOOGLE_PLAY_APP_DETAILS)",
      "type": "string",
      "description": "Enter the ID of the app needed to be scrapped",
      "editor": "textfield"
    },
    "devId": {
      "title": "ID of the developer whose app needed to be scrapped (Works only if action choosen is LIST_APP_STORE_DEVELOPER_APPS)",
      "type": "string",
      "description": "Name of the developer whose app needed to be scrapped",
      "editor": "textfield",
      "prefill": "284882218"
    },
    "selectedCategory": {
      "title": "Choose  Category of App for Play Store  (Works only if action choosen is  LIST_GOOGLE_PLAY_APPS)",
      "type": "string",
      "description": "Choose  Category of App for Play Store ",
      "editor": "select",
      "enum": [
        "BUSINESS",
        "EDUCATION",
        "ENTERTAINMENT",
        "FINANCE",
        "FOOD_AND_DRINK",
        "HEALTH_AND_FITNESS",
        "LIFESTYLE",
        "MEDICAL",
        "PRODUCTIVITY",
        "SHOPPING",
        "SOCIAL",
        "SPORTS",
        "WEATHER"
      ],
      "default": "GAME"
    },
    "priceModel": {
      "title": "Choose pricing model of App  (Works only if action choosen is  LIST_APP_STORE_APPS or LIST_GOOGLE_PLAY_APPS)",
      "type": "string",
      "description": "Select the Pricing Model",
      "editor": "select",
      "enum": ["FREE", "PAID"],
      "default": "FREE"
    },
    "limit": {
      "title": "Number of results",
      "type": "integer",
      "description": "Enter the number of results to be fetched",
      "editor": "number",
      "prefill": 10
    }
  },
  "required": ["action"]
}
